# These are the versions we currently use. 
# Other compatible versions may also work, but are not tested.

pexpect==4.9.0
easydict==1.13
numpy==2.2.5
openai==1.78.1
uvicorn==0.34.2
fastapi==0.115.12
loguru==0.7.3

# Version 1 local BFS-Prover server dependency.
# Install this on the Linux lab machine, preferably in a separate vLLM environment.
vllm
